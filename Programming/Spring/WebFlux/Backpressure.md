Controls the rate of data flow between producer and consumer.

## Why it matters
- Prevents memory overflow
- Protects slow consumers

## Operators
- `.onBackpressureDrop()`
- `.onBackpressureBuffer()`

## Used in
- [[Flux]]
- [[Spring WebFlux]]
