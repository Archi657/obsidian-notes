Allow to do any change like changing or database or input, with just a simple change.

**Tier Architecture** Presentation -> Logic -> Data
(dependency injection 'presentation', abstract classes 'logic' ).

**Ports - Adapters**
No much difference when interacting with a DB or an API. 
